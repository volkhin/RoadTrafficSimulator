'use strict'

require("blanket")({})

require '../helpers'
World = require '../model/world'

describe 'Application', ->

  it 'can run', ->
    # world = new World()
    # world.generateMap()
    # world.carsNumber = 20
    # for i in [0..1000]
      # world.onTick 0.1
